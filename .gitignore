# Dart / Flutter
.dart_tool/
.packages
build/
pubspec.lock
*.log

# Platform folders are generated by CI (flutter create). Commit them only once
# you start adding custom native config (VPN permission, signing, native cores).
/android/
/ios/
/windows/
/linux/
/macos/
/web/

# IDE
.idea/
.vscode/
*.iml

# Never commit real key material.
.env
*.keystore
key.properties

# Go server
server/oversea.db
server/*.db
server/served
server/*.exe
server/bin/
.env.local
.claude/
